package com.dguards.storage.commons.constants

/**
 * Entry points are basically request codes sent by the client app
 * and they are used in both ends to determine the request/response type.
 * */
class EntryPoints {
  companion object {
    const val GET_CONNECTION_MODEL: Int = 1001
  }
}